# Redator Local (Ollama)

Este script roda no seu PC e publica no servidor remoto do blog.

## Exemplo

```bash
npm run redator -- --tema "Como usar IA para automatizar conteúdo"
```

## Opcoes

- `--tema "..."` obrigatorio
- `--modelo "..."` opcional
- `--imagem "https://..."` opcional
